/**
 * @file music.ts
 * @description Music playback configuration for the native Lavende engine.
 */

const musicConfig = {
  defaultVolume:       100,
  autoLeaveMs:         30_000,
  defaultSearchPrefix: 'ytsearch:',
};

export default musicConfig;
